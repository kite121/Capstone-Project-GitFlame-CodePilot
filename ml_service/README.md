# ML Service

The ML service owns model selection, prompts, RAG/context retrieval, issue-to-plan generation, code generation payloads, and repository recommendations.

Sprint 1 uses mock endpoints so the rest of the system can integrate before model serving is finalized.

