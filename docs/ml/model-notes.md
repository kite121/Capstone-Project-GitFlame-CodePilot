# Model Notes

Sprint 1 tracks two possible model routes:

- issue-to-plan and code generation model
- recommendation analysis model

Model selection is an internal system decision and is not configured by the user through `.yml`.

