{
  "request_id": "bench-correction",
  "issue": {
    "id": "issue-105",
    "title": "Record failed webhook deliveries",
    "body": "Persist delivery failures so operators can diagnose integration issues."
  },
  "repository": {
    "id": "webhook-service",
    "default_branch": "main",
    "commit_sha": "bench005"
  },
  "configuration_yaml": "include:\n  - src/**\n",
  "repository_files": [
    {
      "path": "src/webhooks/delivery.py",
      "content": "async def deliver(event, target):\n    await client.post(target, json=event)\n"
    }
  ],
  "previous_plan": "# Previous draft\nStore every request body and response body without limits.",
  "correction_feedback": "Do not store request or response bodies; retain only bounded failure metadata."
}
