{
  "request_id": "bench-token-expiration",
  "issue": {
    "id": "issue-101",
    "title": "Validate token expiration",
    "body": "Reject expired access tokens before returning an authenticated identity."
  },
  "repository": {
    "id": "auth-service",
    "default_branch": "main",
    "commit_sha": "bench001"
  },
  "configuration_yaml": "include:\n  - src/**\n",
  "repository_files": [
    {
      "path": "src/auth.py",
      "content": "def authenticate(token):\n    claims = decode(token)\n    return claims['sub']\n"
    },
    {
      "path": "src/test_auth.py",
      "content": "def test_valid_token():\n    assert authenticate(valid_token()) == 'user-1'\n"
    }
  ]
}
